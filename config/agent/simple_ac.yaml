defaults:
  - base_agent
  - critic: critic
  - actor : network
  - _self_

buffer :
  batch_size: 512
  memory: 100000
  name: uniform
  device : cpu
  seed : ${experiment.seed}

name : simple_ac
tau : 0.0
device : ${experiment.device}

