# core-env

A library of custom gym environments, used for reasearch and testing. 

### Getting Started
```
# create virtual environment
uv venv --python 3.12
source .venv/bin/activate

# install project requirements
uv sync

# run linter
uv run ruff check

# run static type checker
uv run pyright
```
