env:
  name: sim_async_env
  gym_name : SaturationGoals-v0

interaction:
  name: sim_interaction
  obs_period: 00:00:01
  action_period: 00:00:01

experiment:
  exp_name: "saturation-goals"
  seed: 1
  gamma: 0.99
  max_steps: 2000

agent:
  name: greedy_ac
  policy:
    optimizer:
      lr: 0.001


metrics:
  enabled: True

evals:
  name: db
  enabled: True

eval_cfgs:
  actor_critic:
    enabled: True
  monte_carlo:
    enabled: True
    precision: 0.9

pipeline:
  state_constructor:
    defaults:
    - name: multi_trace
      trace_values: [0., 0.75, 0.95]

  reward:
    priorities:
      # Satisfy at least so much saturation
      - op: up_to
        tag: tag-0
        thresh: tag-1

      # But do not go over by too much
      - tags: [tag-0]
        directions: [min]

  tags:
  # meta tags
  - name: terminated
    is_meta: True

  - name: truncated
    is_meta: True

  # observations
  - name: tag-0
    operating_range: [0, 1.0]

  - name: tag-1
    operating_range: [0, 1.0]

  # actions
  - name: action-0
    operating_range: [0, 0.5]
    state_constructor:
      - name: 'null'
    action_constructor:
      - name: identity
