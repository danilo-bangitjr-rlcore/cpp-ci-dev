defaults:
 - event_bus: event_bus

deployment:
  python_executable: python
  python_entrypoint: main.py
  base: config
  config_name: pendulum
  options: [experiment.max_steps=15,metrics.port=5435]

event_bus:
  python_entrypoint: event_bus.py
  base: test/large/smoke/assets
  config_name: event_bus
